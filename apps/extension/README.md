# CinaSeek Companion Extension

浏览器扩展为 PWA 提供捕获选区与后台桥接能力。

## 开发

```bash
yarn workspace @cinaseek/extension dev
```

## 构建

```bash
yarn workspace @cinaseek/extension build
```

打包完成后可在 `apps/extension/dist` 目录中加载到 Chrome/Edge。
